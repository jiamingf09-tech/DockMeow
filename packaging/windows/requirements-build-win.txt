# Windows-specific build requirements for DockMeow.
# Used by build_win.bat (and optionally build_win.ps1).
#
# Differences from requirements-build.txt (macOS/Linux):
#   OpenMM   — no Windows wheel on PyPI; PDB-fixer feature silently
#              disabled on Windows builds (already guarded by try/except).
#   pdbfixer — depends on OpenMM; excluded for the same reason.
#   vina     — has a PyPI wheel for Windows x64.  build_win.bat installs
#              it automatically.  ARM64 users: install via conda instead.

biopython==1.87
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==47.0.0
fonttools==4.62.1
gemmi==0.7.5
meeko==0.7.1
numpy==2.4.4
pdfplumber==0.11.9
Pillow==12.2.0
py3Dmol==2.5.4
PySide6==6.11.0
rdkit==2026.3.1
reportlab==4.5.0
scipy==1.17.1
