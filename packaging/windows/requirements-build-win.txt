# Windows-specific build requirements for DockMeow.
# Used by build_win.bat (and optionally build_win.ps1).
#
# Windows x64 builds use AMD64 Python 3.11. On Windows ARM machines, run this
# x64 build under Windows emulation; native ARM64 wheels are not available for
# every scientific dependency.
#
# Vina Python bindings are optional on Windows. DockMeow uses the bundled
# src/dockmeow/bundled/vina/windows/vina.exe CLI fallback when the Python
# binding is unavailable.

biopython==1.87
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==47.0.0
fonttools==4.62.1
gemmi==0.7.5
meeko==0.7.1
numpy==2.4.4
OpenMM==8.5.1
pdbfixer==1.12.0
pdfplumber==0.11.9
Pillow==12.2.0
py3Dmol==2.5.4
PySide6==6.11.0
rdkit==2026.3.1
reportlab==4.5.0
scipy==1.17.1
